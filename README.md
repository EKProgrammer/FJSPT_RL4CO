# Graph Neural Networks for Scheduling and Logistics

To install dependencies, run
```
pip install -r requirements.txt
````

To run experiments with greedy policies, run all cells in `fjspt_greedy_rl4co.ipynb`.

To run experiments with the HGNN model, run all cells in `fjspt_reinforce_rl4co.ipynb`.
